name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.6.5

      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.x
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Run Core-shared tests
        run: pnpm --filter @zaptime/core-shared test

      - name: Run Core tests
        run: pnpm --filter @zaptime/core test

      - name: Run React Core tests
        run: pnpm --filter @zaptime/react-core test
